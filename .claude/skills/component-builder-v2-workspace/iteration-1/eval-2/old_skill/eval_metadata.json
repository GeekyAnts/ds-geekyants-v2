{
  "eval_id": 2,
  "eval_name": "command-palette-cmdk-recipe",
  "prompt": "Build a command palette (Cmd+K style) that pops open, lets the user type to filter a list of actions, and navigate the results with arrow keys. Like the one in VS Code or Linear.",
  "assertions": [
    "Reuses the shipped Command (cmdk) rather than rebuilding filtering/keyboard nav",
    "Composes inside Dialog (CommandDialog recipe) \u2014 does not rebuild portal/focus trap",
    "Does NOT hand-roll roving tabindex / arrow-key list navigation",
    "Install command uses npm if any  [TARGETED FIX]",
    "BUILD_REPORT explicitly confirms it READ the existing Command/Combobox files"
  ]
}