---
title: Drawer
description: An off-canvas panel that slides in from any viewport edge with focus trapping and Escape dismissal.
level: organism
---

## Placements

The placement prop controls which viewport edge the panel slides in from. Each placement rounds only the inner corners.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction PlacementsDemo() {\n  const [open, setOpen] = useState<'left' | 'right' | 'top' | 'bottom' | null>(null)\n\n  return (\n    <>\n      <div className=\"flex flex-wrap gap-3\">\n        <Button variant=\"outline\" onClick={() => setOpen('left')}>left (open)</Button>\n        <Button variant=\"outline\" onClick={() => setOpen('right')}>right (open)</Button>\n        <Button variant=\"outline\" onClick={() => setOpen('top')}>top (open)</Button>\n        <Button variant=\"outline\" onClick={() => setOpen('bottom')}>bottom (open)</Button>\n      </div>\n      {open && (\n        <Drawer isOpen onClose={() => setOpen(null)} placement={open} title={open + ' drawer'}>\n          <Drawer.Body><p>This drawer slides in from the {open} edge.</p></Drawer.Body>\n          <Drawer.Footer><Button variant=\"primary\" onClick={() => setOpen(null)}>Got it</Button></Drawer.Footer>\n        </Drawer>\n      )}\n    </>\n  )\n}"}>
  <div className="flex flex-wrap gap-3">
    <DrawerDemo placement="left" title="Left drawer" buttonLabel="left (open)" buttonVariant="outline">
      <Drawer.Body><p>This drawer slides in from the <strong>left</strong> edge.</p></Drawer.Body>
      <Drawer.Footer><Button variant="primary">Got it</Button></Drawer.Footer>
    </DrawerDemo>
    <DrawerDemo placement="right" title="Right drawer" buttonLabel="right (open)" buttonVariant="outline">
      <Drawer.Body><p>This drawer slides in from the <strong>right</strong> edge.</p></Drawer.Body>
      <Drawer.Footer><Button variant="primary">Got it</Button></Drawer.Footer>
    </DrawerDemo>
    <DrawerDemo placement="top" title="Top drawer" buttonLabel="top (open)" buttonVariant="outline">
      <Drawer.Body><p>This drawer slides in from the <strong>top</strong> edge.</p></Drawer.Body>
      <Drawer.Footer><Button variant="primary">Got it</Button></Drawer.Footer>
    </DrawerDemo>
    <DrawerDemo placement="bottom" title="Bottom drawer" buttonLabel="bottom (open)" buttonVariant="outline">
      <Drawer.Body><p>This drawer slides in from the <strong>bottom</strong> edge.</p></Drawer.Body>
      <Drawer.Footer><Button variant="primary">Got it</Button></Drawer.Footer>
    </DrawerDemo>
  </div>
</ComponentPreview>

## Sizes

Left/right drawers use size to control panel width; top/bottom drawers use it for height. The full size fills the entire viewport.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction SizesDemo() {\n  const [open, setOpen] = useState<'sm' | 'md' | 'lg' | null>(null)\n\n  return (\n    <>\n      <div className=\"flex flex-wrap gap-3\">\n        <Button variant=\"outline\" onClick={() => setOpen('sm')}>SM (open)</Button>\n        <Button variant=\"outline\" onClick={() => setOpen('md')}>MD (open)</Button>\n        <Button variant=\"outline\" onClick={() => setOpen('lg')}>LG (open)</Button>\n      </div>\n      {open && (\n        <Drawer isOpen onClose={() => setOpen(null)} size={open} placement=\"right\" title={open.toUpperCase() + ' drawer'}>\n          <Drawer.Body><p>This is a {open} right-side drawer.</p></Drawer.Body>\n          <Drawer.Footer><Button variant=\"primary\" onClick={() => setOpen(null)}>Close</Button></Drawer.Footer>\n        </Drawer>\n      )}\n    </>\n  )\n}"}>
  <div className="flex flex-wrap gap-3">
    <DrawerDemo size="sm" placement="right" title="SM drawer" buttonLabel="SM (open)" buttonVariant="outline">
      <Drawer.Body><p>This is a <strong>sm</strong> right-side drawer.</p></Drawer.Body>
      <Drawer.Footer><Button variant="primary">Close</Button></Drawer.Footer>
    </DrawerDemo>
    <DrawerDemo size="md" placement="right" title="MD drawer" buttonLabel="MD (open)" buttonVariant="outline">
      <Drawer.Body><p>This is a <strong>md</strong> right-side drawer (default).</p></Drawer.Body>
      <Drawer.Footer><Button variant="primary">Close</Button></Drawer.Footer>
    </DrawerDemo>
    <DrawerDemo size="lg" placement="right" title="LG drawer" buttonLabel="LG (open)" buttonVariant="outline">
      <Drawer.Body><p>This is a <strong>lg</strong> right-side drawer.</p></Drawer.Body>
      <Drawer.Footer><Button variant="primary">Close</Button></Drawer.Footer>
    </DrawerDemo>
    <DrawerDemo size="xl" placement="right" title="XL drawer" buttonLabel="XL (open)" buttonVariant="outline">
      <Drawer.Body><p>This is an <strong>xl</strong> right-side drawer.</p></Drawer.Body>
      <Drawer.Footer><Button variant="primary">Close</Button></Drawer.Footer>
    </DrawerDemo>
  </div>
</ComponentPreview>

## Default

A basic drawer with a title, body content, and a footer with action buttons.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction DefaultDrawer() {\n  const [open, setOpen] = useState(false)\n\n  return (\n    <>\n      <Button variant=\"primary\" onClick={() => setOpen(true)}>Open drawer</Button>\n      <Drawer isOpen={open} onClose={() => setOpen(false)} title=\"Settings\">\n        <Drawer.Body><p>Panel content goes here.</p></Drawer.Body>\n        <Drawer.Footer>\n          <Button variant=\"ghost\" onClick={() => setOpen(false)}>Cancel</Button>\n          <Button variant=\"primary\" onClick={() => setOpen(false)}>Save changes</Button>\n        </Drawer.Footer>\n      </Drawer>\n    </>\n  )\n}"}>
  <DrawerDemo buttonLabel="Open drawer" title="Settings">
    <Drawer.Body>
      <div className="flex flex-col gap-[var(--spacing-component-lg)]">
        <div className="flex items-center gap-[var(--spacing-component-md)]">
          <span aria-hidden="true" className="text-[var(--color-text-secondary)]"><User size={16} /></span>
          <div>
            <p className="text-body-md text-[var(--color-text-primary)]">Profile</p>
            <p className="text-body-sm text-[var(--color-text-secondary)]">Manage your account details</p>
          </div>
        </div>
        <div className="flex items-center gap-[var(--spacing-component-md)]">
          <span aria-hidden="true" className="text-[var(--color-text-secondary)]"><Bell size={16} /></span>
          <div>
            <p className="text-body-md text-[var(--color-text-primary)]">Notifications</p>
            <p className="text-body-sm text-[var(--color-text-secondary)]">Configure alert preferences</p>
          </div>
        </div>
        <div className="flex items-center gap-[var(--spacing-component-md)]">
          <span aria-hidden="true" className="text-[var(--color-text-secondary)]"><AlertCircle size={16} /></span>
          <div>
            <p className="text-body-md text-[var(--color-text-primary)]">Help and Support</p>
            <p className="text-body-sm text-[var(--color-text-secondary)]">Get help or contact us</p>
          </div>
        </div>
      </div>
    </Drawer.Body>
    <Drawer.Footer>
      <Button variant="ghost">Cancel</Button>
      <Button variant="primary">Save changes</Button>
    </Drawer.Footer>
  </DrawerDemo>
</ComponentPreview>

## Loading

When loading is true the body content is replaced with a skeleton placeholder while the header and footer remain in place.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction LoadingDrawer() {\n  const [open, setOpen] = useState(false)\n\n  return (\n    <>\n      <Button variant=\"outline\" onClick={() => setOpen(true)}>Loading state</Button>\n      <Drawer isOpen={open} onClose={() => setOpen(false)} title=\"Loading content\" loading={true}>\n        <Drawer.Body><p>This content is hidden during loading.</p></Drawer.Body>\n        <Drawer.Footer><Button variant=\"primary\" onClick={() => setOpen(false)}>OK</Button></Drawer.Footer>\n      </Drawer>\n    </>\n  )\n}"}>
  <DrawerDemo buttonLabel="Loading state" buttonVariant="outline" title="Loading content" loading={true}>
    <Drawer.Body><p>This content is hidden during loading.</p></Drawer.Body>
    <Drawer.Footer><Button variant="primary">OK</Button></Drawer.Footer>
  </DrawerDemo>
</ComponentPreview>

## No Title

When no title prop is provided the drawer header omits the heading and the dialog uses aria-label from i18nStrings.drawerLabel as its accessible name.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction NoTitleDrawer() {\n  const [open, setOpen] = useState(false)\n\n  return (\n    <>\n      <Button variant=\"outline\" onClick={() => setOpen(true)}>No title</Button>\n      <Drawer isOpen={open} onClose={() => setOpen(false)}>\n        <Drawer.Body><p>When no title prop is provided, the dialog uses aria-label.</p></Drawer.Body>\n        <Drawer.Footer>\n          <Button variant=\"ghost\" onClick={() => setOpen(false)}>Cancel</Button>\n          <Button variant=\"primary\" onClick={() => setOpen(false)}>OK</Button>\n        </Drawer.Footer>\n      </Drawer>\n    </>\n  )\n}"}>
  <DrawerDemo buttonLabel="No title" buttonVariant="outline">
    <Drawer.Body><p>When no title prop is provided, the dialog uses <code>aria-label</code> as its accessible name.</p></Drawer.Body>
    <Drawer.Footer>
      <Button variant="ghost">Cancel</Button>
      <Button variant="primary">OK</Button>
    </Drawer.Footer>
  </DrawerDemo>
</ComponentPreview>

## Backdrop Disabled

Setting closeOnBackdropClick to false suppresses backdrop-click dismissal. Only the close button or Escape key will close the drawer.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction BackdropDemo() {\n  const [open, setOpen] = useState(false)\n\n  return (\n    <>\n      <Button variant=\"outline\" onClick={() => setOpen(true)}>No backdrop close</Button>\n      <Drawer isOpen={open} onClose={() => setOpen(false)} title=\"Backdrop disabled\" closeOnBackdropClick={false}>\n        <Drawer.Body><p>Clicking outside does not close the drawer.</p></Drawer.Body>\n        <Drawer.Footer><Button variant=\"primary\" onClick={() => setOpen(false)}>Close</Button></Drawer.Footer>\n      </Drawer>\n    </>\n  )\n}"}>
  <DrawerDemo buttonLabel="No backdrop close" buttonVariant="outline" title="Backdrop disabled" closeOnBackdropClick={false}>
    <Drawer.Body><p>Clicking outside this drawer does not close it. Only the close button or Escape key work.</p></Drawer.Body>
    <Drawer.Footer><Button variant="primary">Close</Button></Drawer.Footer>
  </DrawerDemo>
</ComponentPreview>

## Scrollable Content

Drawer.Body grows to fill available panel height and scrolls independently when content overflows. The header and footer remain fixed.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction ScrollableDrawer() {\n  const [open, setOpen] = useState(false)\n\n  return (\n    <>\n      <Button variant=\"outline\" onClick={() => setOpen(true)}>Long content</Button>\n      <Drawer isOpen={open} onClose={() => setOpen(false)} title=\"Scrollable content\" size=\"md\">\n        <Drawer.Body>\n          {Array.from({ length: 8 }).map((_, i) => (\n            <p key={i} className=\"mb-4\">\n              Item {i + 1}: The drawer body scrolls independently when content overflows.\n            </p>\n          ))}\n        </Drawer.Body>\n        <Drawer.Footer><Button variant=\"primary\" onClick={() => setOpen(false)}>Done</Button></Drawer.Footer>\n      </Drawer>\n    </>\n  )\n}"}>
  <DrawerDemo buttonLabel="Long content" buttonVariant="outline" title="Scrollable content" size="md">
    <Drawer.Body>
      {Array.from({ length: 12 }).map((_, i) => (
        <p key={i} className="mb-[var(--spacing-component-md)]">Item {i + 1}: The drawer body scrolls independently when content overflows the panel height. The header and footer remain fixed in place.</p>
      ))}
    </Drawer.Body>
    <Drawer.Footer><Button variant="primary">Done</Button></Drawer.Footer>
  </DrawerDemo>
</ComponentPreview>

## Destructive Action

Use a destructive variant for confirmation dialogs that have permanent consequences.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\nimport { AlertTriangle } from \"lucide-react\"\n\nfunction DestructiveDrawer() {\n  const [open, setOpen] = useState(false)\n\n  return (\n    <>\n      <Button variant=\"destructive\" onClick={() => setOpen(true)}>Danger drawer</Button>\n      <Drawer isOpen={open} onClose={() => setOpen(false)} title=\"Destructive action\">\n        <Drawer.Body>\n          <div className=\"flex gap-3\">\n            <span aria-hidden=\"true\" className=\"text-[var(--color-status-error)] shrink-0 mt-0.5\">\n              <AlertTriangle size={16} />\n            </span>\n            <p>This action is permanent and cannot be undone.</p>\n          </div>\n        </Drawer.Body>\n        <Drawer.Footer>\n          <Button variant=\"ghost\" onClick={() => setOpen(false)}>Cancel</Button>\n          <Button variant=\"destructive\" onClick={() => setOpen(false)}>Delete permanently</Button>\n        </Drawer.Footer>\n      </Drawer>\n    </>\n  )\n}"}>
  <DrawerDemo buttonLabel="Danger drawer" buttonVariant="destructive" title="Destructive action">
    <Drawer.Body>
      <div className="flex gap-[var(--spacing-component-md)]">
        <span aria-hidden="true" className="text-[var(--color-status-error)] shrink-0 mt-0.5"><AlertTriangle size={16} /></span>
        <p>This action is permanent and cannot be undone. All associated data will be removed.</p>
      </div>
    </Drawer.Body>
    <Drawer.Footer>
      <Button variant="ghost">Cancel</Button>
      <Button variant="destructive">Delete permanently</Button>
    </Drawer.Footer>
  </DrawerDemo>
</ComponentPreview>

## Minimal

A minimal drawer with only a title and body content with no footer. Ideal for non-blocking contextual information.

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction MinimalDrawer() {\n  const [open, setOpen] = useState(false)\n\n  return (\n    <>\n      <Button variant=\"outline\" onClick={() => setOpen(true)}>Open minimal drawer</Button>\n      <Drawer isOpen={open} onClose={() => setOpen(false)} title=\"Quick info\" placement=\"right\" size=\"sm\">\n        <Drawer.Body>\n          <p className=\"text-body-sm text-[var(--color-text-secondary)]\">\n            The simplest Drawer usage with a title, body content, and no footer.\n          </p>\n        </Drawer.Body>\n      </Drawer>\n    </>\n  )\n}"}>
  <DrawerDemo buttonLabel="Open minimal drawer" buttonVariant="outline" title="Quick info" placement="right" size="sm">
    <Drawer.Body>
      <p className="text-body-sm text-[var(--color-text-secondary)]">The simplest Drawer usage with a title, body content, and no footer. Ideal for non-blocking contextual information or quick references.</p>
    </Drawer.Body>
  </DrawerDemo>
</ComponentPreview>

## Dark Mode

<ComponentPreview code={"import { useState } from \"react\"\nimport { Drawer, Button } from \"@geeklego/ui\"\n\nfunction DarkModeDrawer() {\n  const [open, setOpen] = useState(false)\n\n  return (\n    <div data-theme=\"dark\" className=\"flex flex-col gap-6 p-8 bg-[var(--color-bg-primary)] rounded-[var(--radius-component-lg)] max-w-2xl\">\n      <Button variant=\"primary\" onClick={() => setOpen(true)}>Open in dark mode</Button>\n      <Drawer isOpen={open} onClose={() => setOpen(false)} title=\"Dark mode drawer\">\n        <Drawer.Body><p>Tokens cascade correctly in dark mode.</p></Drawer.Body>\n        <Drawer.Footer>\n          <Button variant=\"ghost\" onClick={() => setOpen(false)}>Cancel</Button>\n          <Button variant=\"primary\" onClick={() => setOpen(false)}>Confirm</Button>\n        </Drawer.Footer>\n      </Drawer>\n    </div>\n  )\n}"}>
  <div data-theme="dark" className="flex flex-col gap-6 p-8 bg-[var(--color-bg-primary)] rounded-[var(--radius-component-lg)] max-w-2xl">
    <DrawerDemo buttonLabel="Open in dark mode" title="Dark mode drawer">
      <Drawer.Body><p>Tokens cascade correctly in dark mode. The backdrop, panel, text, and footer all use theme-aware semantics.</p></Drawer.Body>
      <Drawer.Footer>
        <Button variant="ghost">Cancel</Button>
        <Button variant="primary">Confirm</Button>
      </Drawer.Footer>
    </DrawerDemo>
  </div>
</ComponentPreview>

## Props

<PropsTable
  rows={[
    { name: "isOpen", type: "boolean", defaultValue: "\u2014", description: "Whether the drawer is visible. Required." },
    { name: "onClose", type: "() => void", defaultValue: "\u2014", description: "Called when the drawer should close (Escape key, backdrop click, or close button). Required." },
    { name: "placement", type: "'left' | 'right' | 'top' | 'bottom'", defaultValue: "'right'", description: "Edge of the viewport the panel slides in from." },
    { name: "size", type: "'sm' | 'md' | 'lg' | 'xl' | 'full'", defaultValue: "'md'", description: "Panel size preset. Left/right controls width. Top/bottom controls height. The full preset fills the entire viewport." },
    { name: "title", type: "string", defaultValue: "\u2014", description: "Title text rendered in the drawer header. Wires aria-labelledby on the dialog. Falls back to i18nStrings.drawerLabel when omitted." },
    { name: "loading", type: "boolean", defaultValue: "false", description: "When true the body content is replaced with a loading skeleton. Dimensions are preserved." },
    { name: "closeOnBackdropClick", type: "boolean", defaultValue: "true", description: "When false clicking the backdrop overlay does not call onClose." },
    { name: "className", type: "string", defaultValue: "\u2014", description: "Additional CSS classes applied to the dialog panel element." },
    { name: "children", type: "ReactNode", defaultValue: "\u2014", description: "Drawer content. Compose Drawer.Body and Drawer.Footer inside." },
    { name: "i18nStrings", type: "DrawerI18nStrings", defaultValue: "\u2014", description: "Per-instance overrides for system strings. Keys: closeLabel (default 'Close'), drawerLabel (default 'Drawer')." },
  ]}
/>

## Accessibility

**Semantic element:** `<div role="dialog" aria-modal="true">` rendered in a portal on `document.body`.

**ARIA attributes:**

| Attribute | Value | Condition |
|---|---|---|
| `role` | `"dialog"` | Always |
| `aria-modal` | `"true"` | Always |
| `aria-labelledby` | Points to the `<h2>` title `id` | When title is provided |
| `aria-label` | `i18nStrings.drawerLabel` | When title is omitted |
| `aria-busy` | `"true"` | When loading is true |

**Focus management:**
- Focus is trapped inside the panel while open via useFocusTrap.
- Escape dismissal is handled by useEscapeDismiss.
- On close focus returns to the element that triggered the drawer.
- The backdrop carries aria-hidden="true" and is a visual affordance only.

### Keyboard Interaction

| Key | Action |
|-----|--------|
| Tab | Moves focus to the next focusable element within the panel (trapped). |
| Shift + Tab | Moves focus to the previous focusable element within the panel. |
| Escape | Closes the drawer and returns focus to the trigger element. |
