---
title: Checkbox
description: An accessible checkbox input with label, indeterminate state, and error feedback.
level: atom
---

## Variants

<ComponentPreview code={"import { useState } from \"react\"\nimport { Checkbox } from \"@geeklego/ui\"\n\nfunction Example() {\n  const [checked, setChecked] = useState(false)\n  return (\n    <Checkbox checked={checked} onChange={(e) => setChecked(e.target.checked)}>\n      Accept terms and conditions\n    </Checkbox>\n  )\n}"}>
  <div className="flex flex-col gap-4">
    <Checkbox>Unchecked</Checkbox>
    <Checkbox defaultChecked>Checked</Checkbox>
    <Checkbox indeterminate>Indeterminate</Checkbox>
  </div>
</ComponentPreview>

## Sizes

<ComponentPreview code={"import { Checkbox } from \"@geeklego/ui\"\n\n<Checkbox size=\"sm\" defaultChecked>Small</Checkbox>\n<Checkbox size=\"md\" defaultChecked>Medium</Checkbox>\n<Checkbox size=\"lg\" defaultChecked>Large</Checkbox>"}>
  <div className="flex flex-col gap-4">
    <Checkbox size="sm" defaultChecked>Small (14 px indicator)</Checkbox>
    <Checkbox size="md" defaultChecked>Medium (16 px indicator)</Checkbox>
    <Checkbox size="lg" defaultChecked>Large (20 px indicator)</Checkbox>
  </div>
</ComponentPreview>

## States

<ComponentPreview code={"import { Checkbox } from \"@geeklego/ui\"\n\n<Checkbox disabled>Disabled</Checkbox>\n<Checkbox defaultChecked disabled>Disabled checked</Checkbox>\n<Checkbox error>Error</Checkbox>\n<Checkbox defaultChecked error>Error checked</Checkbox>\n<Checkbox required>Required</Checkbox>"}>
  <div className="flex flex-col gap-4">
    <Checkbox>Default (unchecked)</Checkbox>
    <Checkbox defaultChecked>Checked</Checkbox>
    <Checkbox indeterminate>Indeterminate</Checkbox>
    <Checkbox disabled>Disabled unchecked</Checkbox>
    <Checkbox defaultChecked disabled>Disabled checked</Checkbox>
    <Checkbox indeterminate disabled>Disabled indeterminate</Checkbox>
    <Checkbox error>Error unchecked</Checkbox>
    <Checkbox defaultChecked error>Error checked</Checkbox>
    <Checkbox required>Required field</Checkbox>
  </div>
</ComponentPreview>

## Long Label

<ComponentPreview code={"<div className=\"flex flex-col gap-6 w-96\">\n  <Checkbox defaultChecked>I understand and agree that by checking this box, I accept the terms and conditions, privacy policy, and cookie preferences as outlined in our comprehensive documentation.</Checkbox>\n  <Checkbox>This is an exceptionally long checkbox label that contains a lot of text and should demonstrate how the component handles layout when the content extends beyond a single line.</Checkbox>\n  <Checkbox required>I confirm that all information provided above is accurate and complete to the best of my knowledge and belief.</Checkbox>\n</div>"}>
  <div className="flex flex-col gap-6 w-96">
    <Checkbox defaultChecked>I understand and agree that by checking this box, I accept the terms and conditions, privacy policy, and cookie preferences as outlined in our comprehensive documentation.</Checkbox>
    <Checkbox>This is an exceptionally long checkbox label that contains a lot of text and should demonstrate how the component handles layout when the content extends beyond a single line.</Checkbox>
    <Checkbox required>I confirm that all information provided above is accurate and complete to the best of my knowledge and belief.</Checkbox>
  </div>
</ComponentPreview>

## Dark Mode

<ComponentPreview code={"<div data-theme=\"dark\" className=\"bg-[var(--color-bg-primary)] p-8 rounded-[var(--radius-component-lg)] max-w-2xl\">\n  <div className=\"flex flex-col gap-4\">\n    <Checkbox>Unchecked</Checkbox>\n    <Checkbox defaultChecked>Checked</Checkbox>\n    <Checkbox error>Error</Checkbox>\n  </div>\n</div>"}>
  <div data-theme="dark" className="bg-[var(--color-bg-primary)] p-8 rounded-[var(--radius-component-lg)] max-w-2xl">
    <div className="flex flex-col gap-4">
      <Checkbox>Unchecked in dark mode</Checkbox>
      <Checkbox defaultChecked>Checked in dark mode</Checkbox>
      <Checkbox indeterminate>Indeterminate in dark mode</Checkbox>
      <Checkbox disabled>Disabled in dark mode</Checkbox>
      <Checkbox error>Error in dark mode</Checkbox>
    </div>
  </div>
</ComponentPreview>

## Props

<PropsTable
  rows={[
    { name: "checked", type: "boolean", defaultValue: "false", description: "Whether the checkbox is checked. Control via onChange." },
    { name: "defaultChecked", type: "boolean", defaultValue: "false", description: "Initial checked state for uncontrolled usage." },
    { name: "indeterminate", type: "boolean", defaultValue: "false", description: "Tri-state — partially selected. Visually overrides checked display with a dash." },
    { name: "size", type: "'sm' | 'md' | 'lg'", defaultValue: "'md'", description: "Visual size of the indicator box." },
    { name: "error", type: "boolean", defaultValue: "false", description: "Marks the field as invalid — shifts border to error color." },
    { name: "disabled", type: "boolean", defaultValue: "false", description: "Disables interaction." },
    { name: "required", type: "boolean", defaultValue: "false", description: "Marks the field as required." },
    { name: "children", type: "ReactNode", defaultValue: "—", description: "Label text or elements rendered alongside the indicator. Without children, supply aria-label." },
    { name: "onChange", type: "ChangeEventHandler", defaultValue: "—", description: "Callback fired when the checked state changes." },
    { name: "className", type: "string", defaultValue: "—", description: "Additional CSS classes." },
  ]}
/>

## Accessibility

- Renders a native `<input type="checkbox">` — full browser accessibility support
- `aria-checked="mixed"` set when `indeterminate` is true
- `aria-invalid="true"` set when `error` is true
- `aria-required="true"` set when `required` is true
- `aria-disabled="true"` set when `disabled` is true
- Label is associated to the input via a wrapping `<label>` element
- When no `children` label is provided, supply `aria-label` to describe the checkbox
- `focus-visible` focus ring for keyboard navigation

### Keyboard Interaction

| Key | Action |
|-----|--------|
| Tab | Moves focus to the checkbox |
| Space | Toggles the checked state |
