---
title: Calendar
description: Standalone calendar grid for date selection with month navigation, keyboard support, and full i18n.
level: molecule
---

## Variants

<ComponentPreview code={"import { Calendar } from \"@geeklego/ui\"\n\n<div className=\"flex flex-col gap-6\">\n  <Calendar value={new Date(2026, 4, 15)} />\n  <Calendar />\n</div>"}>
  <div className="flex flex-col gap-6">
    <Calendar value={new Date(2026, 4, 15)} />
    <Calendar />
  </div>
</ComponentPreview>

## First Day of Week

<ComponentPreview code={"import { Calendar } from \"@geeklego/ui\"\n\n<div className=\"flex flex-col gap-6\">\n  <Calendar value={new Date(2026, 4, 15)} firstDayOfWeek={1} />\n  <Calendar value={new Date(2026, 4, 15)} firstDayOfWeek={0} />\n</div>"}>
  <div className="flex flex-col gap-6">
    <Calendar value={new Date(2026, 4, 15)} firstDayOfWeek={1} />
    <Calendar value={new Date(2026, 4, 15)} firstDayOfWeek={0} />
  </div>
</ComponentPreview>

## Selected

<ComponentPreview code={"import { Calendar } from \"@geeklego/ui\"\n\n<Calendar value={new Date(2026, 4, 15)} />"}>
  <Calendar value={new Date(2026, 4, 15)} />
</ComponentPreview>

## Date Constraints

<ComponentPreview code={"import { Calendar } from \"@geeklego/ui\"\n\n<Calendar\n  value={new Date(2026, 4, 15)}\n  min={new Date(2026, 4, 10)}\n  max={new Date(2026, 4, 20)}\n/>"}>
  <Calendar
    value={new Date(2026, 4, 15)}
    min={new Date(2026, 4, 10)}
    max={new Date(2026, 4, 20)}
  />
</ComponentPreview>

## Outside Month

<ComponentPreview code={"import { Calendar } from \"@geeklego/ui\"\n\n<Calendar\n  displayMonth={{ year: 2026, month: 3 }}\n  value={new Date(2026, 3, 15)}\n/>"}>
  <Calendar
    displayMonth={{ year: 2026, month: 3 }}
    value={new Date(2026, 3, 15)}
  />
</ComponentPreview>

## Dark Mode

<ComponentPreview code={"import { Calendar } from \"@geeklego/ui\"\n\n<div\n  data-theme=\"dark\"\n  className=\"flex flex-col gap-6 p-8 bg-[var(--color-bg-primary)] rounded-[var(--radius-component-lg)] max-w-2xl\"\n>\n  <Calendar value={new Date(2026, 4, 15)} />\n  <Calendar\n    value={new Date(2026, 4, 15)}\n    min={new Date(2026, 4, 10)}\n    max={new Date(2026, 4, 20)}\n  />\n</div>"}>
  <div
    data-theme="dark"
    className="flex flex-col gap-6 p-8 bg-[var(--color-bg-primary)] rounded-[var(--radius-component-lg)] max-w-2xl"
  >
    <Calendar value={new Date(2026, 4, 15)} />
    <Calendar
      value={new Date(2026, 4, 15)}
      min={new Date(2026, 4, 10)}
      max={new Date(2026, 4, 20)}
    />
  </div>
</ComponentPreview>

## Props

<PropsTable
  rows={[
    { name: "value", type: "Date | null", defaultValue: "—", description: "Currently selected date (controlled)." },
    { name: "displayMonth", type: "{ year: number; month: number }", defaultValue: "Current month", description: "Month/year to display." },
    { name: "onChange", type: "(date: Date) => void", defaultValue: "—", description: "Fired when the user selects a date." },
    { name: "onMonthChange", type: "(month: { year: number; month: number }) => void", defaultValue: "—", description: "Fired when the user navigates to a different month." },
    { name: "min", type: "Date", defaultValue: "—", description: "Earliest selectable date. Days before this are disabled." },
    { name: "max", type: "Date", defaultValue: "—", description: "Latest selectable date. Days after this are disabled." },
    { name: "firstDayOfWeek", type: "0 | 1", defaultValue: "1", description: "First day of week: 0 = Sunday, 1 = Monday." },
    { name: "i18nStrings", type: "CalendarI18nStrings", defaultValue: "—", description: "Internationalisation strings for system-generated text." },
  ]}
/>

## Accessibility

- Uses `<table>` with `<thead>` and `<tbody>` for semantic grid structure
- Weekday headers use `<th scope="col">` with `abbr` for full names
- Day cells are `<button>` elements with `aria-selected`, `aria-current="date"` for today, and `aria-disabled` for out-of-range days
- Each day button has a full-text `aria-label` (e.g. "15 May 2026, Today")
- Month/year label has `aria-live="polite"` to announce month changes
- Table has `aria-label` with month and year

### Keyboard Interaction

| Key | Action |
|-----|--------|
| ArrowLeft / ArrowRight | Previous / next day |
| ArrowUp / ArrowDown | Previous / next week |
| Home / End | Start / end of week |
| PageUp / PageDown | Previous / next month |
| Shift+PageUp / Shift+PageDown | Previous / next year |
| Enter / Space | Selects focused date |
